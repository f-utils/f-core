[project]
name = "f-core"
version = "0.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "f",
]

[tool.uv.sources]
f = { git = "https://github.com/f-utils/f" }
